const { expect } = require('chai');


describe('wagtail utilities', function() {
  describe('thumbnail picker', function() {
    it('picks the lead asset if it is an image');

    it('picks the lead asset default if it is a gallery');

    it('picks the listing image if there is one');
  });
});
